@* @model TicketManagementApp.Models.Ticket

@{
    ViewData["Title"] = "Delete Ticket";
}

<style>
    .page-header {
        margin-bottom: 2rem;
    }

    .breadcrumb-nav {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: #64748b;
        font-size: 0.875rem;
        margin-bottom: 1rem;
    }

        .breadcrumb-nav a {
            color: #6366f1;
            text-decoration: none;
            transition: color 0.3s ease;
        }

            .breadcrumb-nav a:hover {
                color: #4f46e5;
            }

    .page-title {
        font-size: 2rem;
        font-weight: 700;
        color: #1e293b;
        margin: 0;
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

    .delete-container {
        max-width: 800px;
    }

    .warning-card {
        background: white;
        border-radius: 16px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        overflow: hidden;
        margin-bottom: 2rem;
    }

    .warning-header {
        background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
        padding: 2rem;
        text-align: center;
        color: white;
    }

    .warning-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
        animation: pulse 2s infinite;
    }



    50% {
        opacity: 0.8;
        transform: scale(1.05);
    }

    }

    .warning-title {
        font-size: 1.75rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
    }

    .warning-subtitle {
        font-size: 1rem;
        opacity: 0.95;
    }

    .warning-body {
        padding: 2rem;
    }

    .alert-box {
        background: #fef2f2;
        border: 2px solid #fecaca;
        padding: 1.5rem;
        border-radius: 12px;
        margin-bottom: 2rem;
        display: flex;
        gap: 1rem;
    }

    .alert-icon {
        color: #dc2626;
        font-size: 1.5rem;
        flex-shrink: 0;
    }

    .alert-content {
        flex: 1;
    }

    .alert-title {
        font-weight: 700;
        color: #991b1b;
        margin-bottom: 0.5rem;
        font-size: 1.1rem;
    }

    .alert-text {
        color: #991b1b;
        line-height: 1.6;
    }

    .ticket-preview {
        background: #f8fafc;
        border: 2px solid #e2e8f0;
        border-radius: 12px;
        padding: 1.5rem;
    }

    .preview-header {
        display: flex;
        justify-content: space-between;
        align-items: start;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 2px solid #e2e8f0;
    }

    .preview-title {
        font-size: 1.25rem;
        font-weight: 700;
        color: #1e293b;
        margin-bottom: 0.5rem;
    }

    .preview-id {
        font-size: 0.875rem;
        color: #64748b;
    }

    .preview-badges {
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
    }

    .detail-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1.5rem;
    }

    .detail-item {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .detail-label {
        font-size: 0.75rem;
        font-weight: 600;
        color: #64748b;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .detail-value {
        font-weight: 600;
        color: #1e293b;
    }

    .status-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        padding: 0.4rem 0.75rem;
        border-radius: 6px;
        font-size: 0.8rem;
        font-weight: 600;
    }

    .status-1 {
        background: #dcfce7;
        color: #16a34a;
    }

    .status-2 {
        background: #fef3c7;
        color: #d97706;
    }

    .status-3 {
        background: #fed7aa;
        color: #f97316;
    }

    .status-4 {
        background: #fee2e2;
        color: #ef4444;
    }

    .status-5 {
        background: #fecaca;
        color: #dc2626;
    }

    .category-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        padding: 0.4rem 0.75rem;
        border-radius: 6px;
        font-size: 0.8rem;
        font-weight: 600;
        border: 2px solid;
    }

    .category-bug {
        background: #fee2e2;
        color: #dc2626;
        border-color: #fca5a5;
    }

    .category-improvement {
        background: #dbeafe;
        color: #2563eb;
        border-color: #93c5fd;
    }

    .category-access {
        background: #fef3c7;
        color: #d97706;
        border-color: #fcd34d;
    }

    .category-feature {
        background: #e9d5ff;
        color: #9333ea;
        border-color: #d8b4fe;
    }

    .form-actions {
        display: flex;
        gap: 1rem;
        padding-top: 2rem;
    }

    .btn {
        padding: 0.875rem 2rem;
        border-radius: 10px;
        font-weight: 600;
        font-size: 1rem;
        cursor: pointer;
        transition: all 0.3s ease;
        border: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
        justify-content: center;
        flex: 1;
    }

    .btn-danger {
        background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
        color: white;
        box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
    }

        .btn-danger:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(239, 68, 68, 0.4);
        }

    .btn-secondary {
        background: #f1f5f9;
        color: #64748b;
    }

        .btn-secondary:hover {
            background: #e2e8f0;
            color: #475569;
        }



    .form-actions {
        flex-direction: column-reverse;
    }

    }
</style>

<div class="page-header">
    <nav class="breadcrumb-nav">
        <a asp-action="Index">
            <i class="fas fa-ticket-alt"></i> Tickets
        </a>
        <span>/</span>
        <span>Delete</span>
    </nav>
    <h1 class="page-title">
        <i class="fas fa-trash-alt"></i>
        Delete Ticket
    </h1>
</div>

<div class="delete-container">
    <div class="warning-card">
        <div class="warning-header">
            <div class="warning-icon">
                <i class="fas fa-exclamation-triangle"></i>
            </div>
            <h2 class="warning-title">Confirm Deletion</h2>
            <p class="warning-subtitle">This action cannot be undone</p>
        </div>

        <div class="warning-body">
            <div class="alert-box">
                <div class="alert-icon">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <div class="alert-content">
                    <div class="alert-title">Are you sure you want to delete this ticket?</div>
                    <div class="alert-text">
                        Once deleted, this ticket and all its associated data will be permanently removed from the system.
                        This action is irreversible and cannot be recovered.
                    </div>
                </div>
            </div>

            <div class="ticket-preview">
                <div class="preview-header">
                    <div>
                        <div class="preview-id">Ticket #@Model.Id</div>
                        <div class="preview-title">@Model.Title</div>
                    </div>
                    <div class="preview-badges">
                        @{
                            var statusValue = Model.Status ?? "1";
                            var statusInt = int.TryParse(statusValue, out var parsed) ? parsed : 1;
                            var statusClass = $"status-{statusInt}";
                            var statusText = statusInt switch
                            {
                                1 => "Low",
                                2 => "Medium",
                                3 => "High",
                                4 => "Urgent",
                                5 => "Critical",
                                _ => "Low"
                            };

                            var categoryClass = Model.Category?.ToLower() switch
                            {
                                "bug" => "category-bug",
                                "improvement" => "category-improvement",
                                "access" => "category-access",
                                "feature" => "category-feature",
                                _ => "category-bug"
                            };
                            var categoryIcon = Model.Category?.ToLower() switch
                            {
                                "bug" => "fa-bug",
                                "improvement" => "fa-arrow-up",
                                "access" => "fa-key",
                                "feature" => "fa-star",
                                _ => "fa-tag"
                            };
                        }
                        <span class="status-badge @statusClass">
                            <i class="fas fa-circle" style="font-size: 0.5rem;"></i>
                            @statusText
                        </span>
                        <span class="category-badge @categoryClass">
                            <i class="fas @categoryIcon"></i>
                            @Model.Category
                        </span>
                    </div>
                </div>

                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-label">
                            <i class="fas fa-align-left"></i> Description
                        </div>
                        <div class="detail-value">@Model.Description</div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-label">
                            <i class="far fa-calendar-alt"></i> Created Date
                        </div>
                        <div class="detail-value">@Model.CreatedAt.ToString("MMMM dd, yyyy 'at' hh:mm tt")</div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-label">
                            <i class="fas fa-user"></i> Created By
                        </div>
                        <div class="detail-value">@(Model.CreatedByUser?.UserName ?? "Unknown")</div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-label">
                            <i class="fas fa-user-check"></i> Assigned To
                        </div>
                        <div class="detail-value">@(Model.AssignedToUser?.UserName ?? "Unassigned")</div>
                    </div>
                </div>
            </div>

            <form asp-action="Delete" method="post">
                <input type="hidden" asp-for="Id" />
                <div class="form-actions">
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i>
                        Cancel
                    </a>
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash-alt"></i>
                        Yes, Delete This Ticket
                    </button>
                </div>
            </form>
        </div>
    </div>
</div> *@

@model TicketManagementApp.Models.Ticket

@{
    ViewData["Title"] = "Delete Ticket";
}

<style>
    .page-header {
        margin-bottom: 2rem;
    }

    . breadcrumb-nav {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: #64748b;
        font-size: 0. 875rem;
        margin-bottom: 1rem;
    }

        .breadcrumb-nav a {
            color: #6366f1;
            text-decoration: none;
            transition: color 0.3s ease;
        }

            .breadcrumb-nav a:hover {
                color: #4f46e5;
            }

    . page-title {
        font-size: 2rem;
        font-weight: 700;
        color: #1e293b;
        margin: 0;
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

    .delete-container {
        max-width: 800px;
    }

    .warning-card {
        background: white;
        border-radius: 16px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        overflow: hidden;
        margin-bottom: 2rem;
    }

    .warning-header {
        background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
        padding: 2rem;
        text-align: center;
        color: white;
    }

    .warning-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
        animation: pulse 2s infinite;
    }

    @@keyframes pulse {
        0%, 100% {
            opacity: 1;
            transform: scale(1);
        }

        50% {
            opacity: 0.8;
            transform: scale(1.05);
        }
    }

    . warning-title {
        font-size: 1.75rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
    }

    .warning-subtitle {
        font-size: 1rem;
        opacity: 0. 95;
    }

    . warning-body {
        padding: 2rem;
    }

    .alert-box {
        background: #fef2f2;
        border: 2px solid #fecaca;
        padding: 1.5rem;
        border-radius: 12px;
        margin-bottom: 2rem;
        display: flex;
        gap: 1rem;
    }

    . alert-icon {
        color: #dc2626;
        font-size: 1.5rem;
        flex-shrink: 0;
    }

    .alert-content {
        flex: 1;
    }

    .alert-title {
        font-weight: 700;
        color: #991b1b;
        margin-bottom: 0.5rem;
        font-size: 1.1rem;
    }

    .alert-text {
        color: #991b1b;
        line-height: 1.6;
    }

    .ticket-preview {
        background: #f8fafc;
        border: 2px solid #e2e8f0;
        border-radius: 12px;
        padding: 1.5rem;
    }

    .preview-header {
        display: flex;
        justify-content: space-between;
        align-items: start;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 2px solid #e2e8f0;
    }

    . preview-title {
        font-size: 1.25rem;
        font-weight: 700;
        color: #1e293b;
        margin-bottom: 0.5rem;
    }

    .preview-id {
        font-size: 0.875rem;
        color: #64748b;
    }

    .preview-badges {
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
    }

    .detail-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1.5rem;
    }

    . detail-item {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .detail-label {
        font-size: 0.75rem;
        font-weight: 600;
        color: #64748b;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .detail-value {
        font-weight: 600;
        color: #1e293b;
    }

    .status-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        padding: 0. 4rem 0.75rem;
        border-radius: 6px;
        font-size: 0.8rem;
        font-weight: 600;
    }

    .status-open {
        background: #dbeafe;
        color: #2563eb;
    }

    .status-inprogress {
        background: #fef3c7;
        color: #f59e0b;
    }

    .status-resolved {
        background: #d1fae5;
        color: #10b981;
    }

    .status-closed {
        background: #f3f4f6;
        color: #6b7280;
    }

    .urgency-badge {
        display: inline-flex;
        align-items: center;
        gap: 0. 4rem;
        padding: 0.4rem 0.75rem;
        border-radius: 6px;
        font-size: 0.8rem;
        font-weight: 600;
    }

    .urgency-1 {
        background: #dcfce7;
        color: #16a34a;
    }

    .urgency-2 {
        background: #fef3c7;
        color: #d97706;
    }

    .urgency-3 {
        background: #fed7aa;
        color: #f97316;
    }

    .urgency-4 {
        background: #fee2e2;
        color: #ef4444;
    }

    .urgency-5 {
        background: #fecaca;
        color: #dc2626;
    }

    .category-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        padding: 0.4rem 0.75rem;
        border-radius: 6px;
        font-size: 0.8rem;
        font-weight: 600;
        border: 2px solid;
    }

    .category-bug {
        background: #fee2e2;
        color: #dc2626;
        border-color: #fca5a5;
    }

    .category-improvement {
        background: #dbeafe;
        color: #2563eb;
        border-color: #93c5fd;
    }

    .category-access {
        background: #fef3c7;
        color: #d97706;
        border-color: #fcd34d;
    }

    .category-feature {
        background: #e9d5ff;
        color: #9333ea;
        border-color: #d8b4fe;
    }

    .form-actions {
        display: flex;
        gap: 1rem;
        padding-top: 2rem;
    }

    .btn {
        padding: 0.875rem 2rem;
        border-radius: 10px;
        font-weight: 600;
        font-size: 1rem;
        cursor: pointer;
        transition: all 0.3s ease;
        border: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
        justify-content: center;
        flex: 1;
    }

    .btn-danger {
        background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
        color: white;
        box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
    }

        .btn-danger:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(239, 68, 68, 0.4);
        }

    .btn-secondary {
        background: #f1f5f9;
        color: #64748b;
    }

        .btn-secondary:hover {
            background: #e2e8f0;
            color: #475569;
        }

    @@media (max-width: 768px) {
        .detail-grid {
            grid-template-columns: 1fr;
        }

        .preview-header {
            flex-direction: column;
            gap: 1rem;
        }

        .form-actions {
            flex-direction: column-reverse;
        }
    }
</style>

<div class="page-header">
    <nav class="breadcrumb-nav">
        <a asp-action="Index">
            <i class="fas fa-ticket-alt"></i> Tickets
        </a>
        <span>/</span>
        <span>Delete</span>
    </nav>
    <h1 class="page-title">
        <i class="fas fa-trash-alt"></i>
        Delete Ticket
    </h1>
</div>

<div class="delete-container">
    <div class="warning-card">
        <div class="warning-header">
            <div class="warning-icon">
                <i class="fas fa-exclamation-triangle"></i>
            </div>
            <h2 class="warning-title">Confirm Deletion</h2>
            <p class="warning-subtitle">This action cannot be undone</p>
        </div>

        <div class="warning-body">
            <div class="alert-box">
                <div class="alert-icon">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <div class="alert-content">
                    <div class="alert-title">Are you sure you want to delete this ticket?</div>
                    <div class="alert-text">
                        Once deleted, this ticket and all its associated data will be permanently removed from the system.
                        This action is irreversible and cannot be recovered.
                    </div>
                </div>
            </div>

            <div class="ticket-preview">
                <div class="preview-header">
                    <div>
                        <div class="preview-id">Ticket #@Model. Id</div>
                        <div class="preview-title">@Model.Title</div>
                    </div>
                    <div class="preview-badges">
                        @{
                            // Status Badge
                            var statusClass = Model.Status?.ToLower() switch
                            {
                                "open" => "status-open",
                                "inprogress" => "status-inprogress",
                                "resolved" => "status-resolved",
                                "closed" => "status-closed",
                                _ => "status-open"
                            };
                            var statusText = Model.Status switch
                            {
                                "Open" => "Open",
                                "InProgress" => "In Progress",
                                "Resolved" => "Resolved",
                                "Closed" => "Closed",
                                _ => "Open"
                            };
                            var statusIcon = Model.Status?.ToLower() switch
                            {
                                "open" => "fa-folder-open",
                                "inprogress" => "fa-spinner",
                                "resolved" => "fa-check-circle",
                                "closed" => "fa-times-circle",
                                _ => "fa-folder-open"
                            };

                            // Urgency Badge
                            var urgencyClass = $"urgency-{Model.UrgencyLevel}";
                            var urgencyText = Model.UrgencyLevel switch
                            {
                                1 => "Low",
                                2 => "Medium",
                                3 => "High",
                                4 => "Urgent",
                                5 => "Critical",
                                _ => "Low"
                            };

                            // Category Badge
                            var categoryClass = Model.Category?.ToLower() switch
                            {
                                "bug" => "category-bug",
                                "improvement" => "category-improvement",
                                "access" => "category-access",
                                "feature" => "category-feature",
                                _ => "category-bug"
                            };
                            var categoryIcon = Model.Category?.ToLower() switch
                            {
                                "bug" => "fa-bug",
                                "improvement" => "fa-arrow-up",
                                "access" => "fa-key",
                                "feature" => "fa-star",
                                _ => "fa-tag"
                            };
                        }
                        <span class="status-badge @statusClass">
                            <i class="fas @statusIcon"></i>
                            @statusText
                        </span>
                        <span class="urgency-badge @urgencyClass">
                            <i class="fas fa-circle" style="font-size: 0.5rem;"></i>
                            @urgencyText
                        </span>
                        <span class="category-badge @categoryClass">
                            <i class="fas @categoryIcon"></i>
                            @Model.Category
                        </span>
                    </div>
                </div>

                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-label">
                            <i class="fas fa-align-left"></i> Description
                        </div>
                        <div class="detail-value">
                            @if (string.IsNullOrWhiteSpace(Model.Description))
                            {
                                <em style="color: #94a3b8;">No description provided</em>
                            }
                            else
                            {
                                @Model.Description
                            }
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-label">
                            <i class="far fa-calendar-alt"></i> Created Date
                        </div>
                        <div class="detail-value">@Model.CreatedAt.ToString("MMMM dd, yyyy 'at' hh:mm tt")</div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-label">
                            <i class="fas fa-user"></i> Created By
                        </div>
                        <div class="detail-value">@(Model.CreatedByUser?.UserName ?? "Unknown")</div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-label">
                            <i class="fas fa-user-check"></i> Assigned To
                        </div>
                        <div class="detail-value">@(Model.AssignedToUser?.UserName ?? "Unassigned")</div>
                    </div>
                </div>
            </div>

            <form asp-action="Delete" method="post">
                <input type="hidden" asp-for="Id" />
                <div class="form-actions">
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i>
                        Cancel
                    </a>
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash-alt"></i>
                        Yes, Delete This Ticket
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>